services:
  - type: web
    name: pdf-to-excel-converter
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.2
    autoDeploy: true 